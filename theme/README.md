# Shopify Theme Workspace

This folder is reserved for future Shopify implementation files.

Expected structure:

- `layout/`
- `templates/`
- `sections/`
- `snippets/`
- `assets/`
- `config/`

This keeps the repo ready for real storefront build work without forcing a full scaffold too early.
